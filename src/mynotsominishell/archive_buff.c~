#include "mynotsominishell.h"
/*
 *
 *
 *
 */
void archive_buff()
{
  t_node temp;
  
  temp = gl_env.currNode;
  gl_env.currentNode = new_node("");
  temp->next = gl_env.currentNode;
  gl_env.currNode->prev = temp;

}
